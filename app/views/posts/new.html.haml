%h2 New post

= form_for resource, :url => posts_path do |form|
  = render :partial => "posts/forms/#{resource_class.to_s.downcase}", :locals => {:form => form}
  = render :partial => form
  = form.submit "Create"
  or
  = link_to 'Cancel', posts_path
